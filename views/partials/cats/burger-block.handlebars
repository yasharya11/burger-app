<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-eaten="{{eaten}}">
		{{#if eaten}}Eat Burger{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
